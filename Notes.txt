Project from
Complete NodeJS Dev in 2022
https://www.udemy.com/course/complete-nodejs-developer-zero-to-mastery/learn/lecture/26018310#content

Copied the frontend from the .zip
in client run: npm install
npm start

localhost:3000

cd server
npm init -y
npm install express
npm install --save-dev nodemon
...
modify package.json... name, description, scripts section for "watch"

In Server, Create a src folder,
move server.js, models and routes into src folder.
then fix path 
"main": "src/server.js",
"start": "node src/server.js"
into package.json

--------------------------------------------

To set PORT in your package.json on Windows, you'll want to write:

"start": "set PORT=5000&& node src/server.js"

Instead of:

"start": "PORT=5000 node src/server.js"

--------------------------------------------

Add to Github

echo "# NasaLaunch" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/danfilion/NasaLaunch.git
git push -u origin main

222






